# 💳 다음 소비 예측 대시보드

Streamlit 기반 대화형 웹 대시보드

## 🚀 빠른 시작

### 1. 환경 설정
```bash
# 가상환경 생성 (선택)
conda create -n dashboard python=3.11 -y
conda activate dashboard

# 패키지 설치
pip install -r requirements_dashboard.txt
```

### 2. 대시보드 실행
```bash
streamlit run app_dashboard.py
```

자동으로 브라우저가 열리며 `http://localhost:8501`에서 확인 가능합니다.

---

## 📋 사용 방법

### 1단계: 모델 학습 (최초 1회)
```bash
./run_sequence_pipeline.sh
```

학습된 모델이 `03_models/06_sequence/`에 저장됩니다.

### 2단계: CSV 파일 준비

**파일 형식**:
```csv
날짜,시간,타입,대분류,소분류,내용,금액,화폐,결제수단,메모
2025-12-02,19:28,지출,생활,편의점,GS25,-8400,KRW,KB카드,
2025-12-02,14:46,수입,투자,미분류,주식,500000,KRW,계좌,
```

**필수 컬럼**:
- `날짜`: YYYY-MM-DD
- `시간`: HH:MM
- `대분류`: 소비 카테고리
- `금액`: 숫자 (양수/음수)

**인코딩**: EUC-KR 또는 UTF-8 자동 감지

### 3단계: 대시보드에서 업로드

1. 브라우저에서 대시보드 열기
2. 왼쪽 "파일 선택" 버튼 클릭
3. CSV 파일 업로드
4. "🎯 다음 구매 예측하기" 버튼 클릭

---

## 🎨 기능

### 📊 데이터 분석
- 최근 거래 내역 표시
- 카테고리 자동 매핑
- 거래 통계 요약

### 🎯 예측
- 다음 구매 카테고리 예측
- 카테고리별 확률 분포
- 시각화 차트 (Plotly)

### 🔒 개인정보 보호
- 데이터는 서버에 저장되지 않음
- 브라우저 메모리에서만 처리
- 세션 종료 시 자동 삭제

---

## 🗺️ 카테고리 매핑

개인 데이터 카테고리 → 모델 카테고리 (6개)

| 개인 카테고리 | 모델 카테고리 |
|-------------|-------------|
| 식비, 카페/간식, 술/유흥 | **외식** |
| 온라인쇼핑, 패션/쇼핑, 뷰티/미용 | **쇼핑** |
| 생활, 생필품, 의료/건강 | **생활** |
| 교통 | **교통** |
| 자동차, 주유 | **주유** |
| 식료품 구매 | **식료품** |

---

## ⚙️ 고급 설정

### 포트 변경
```bash
streamlit run app_dashboard.py --server.port 8080
```

### 외부 접속 허용
```bash
streamlit run app_dashboard.py --server.address 0.0.0.0
```

### 테마 변경
```bash
# .streamlit/config.toml 생성
mkdir -p .streamlit
cat > .streamlit/config.toml << EOF
[theme]
primaryColor="#FF4B4B"
backgroundColor="#0E1117"
secondaryBackgroundColor="#262730"
textColor="#FAFAFA"
font="sans serif"
EOF
```

---

## 📂 파일 구조

```
ibm_data2/
├── app_dashboard.py                    # 대시보드 메인
├── requirements_dashboard.txt          # 패키지 목록
├── 2024-12-03~2025-12-03.csv          # 샘플 데이터
└── 03_models/06_sequence/
    ├── best_xgboost_sequence_*.joblib  # 학습된 모델
    └── metadata_*.json                 # 메타정보
```

---

## 🐛 문제 해결

### 모델을 찾을 수 없음
```bash
# 모델 학습 실행
./run_sequence_pipeline.sh

# 모델 파일 확인
ls -la 03_models/06_sequence/
```

### 인코딩 오류
- 파일을 UTF-8로 저장하거나
- 대시보드가 자동으로 EUC-KR 감지 시도

### Streamlit 설치 오류
```bash
pip install --upgrade pip
pip install streamlit --no-cache-dir
```

---

## 📊 예측 성능

학습 데이터에 따라 다르지만 일반적으로:
- **정확도**: 30-50%
- **F1 Score**: 0.35-0.55
- **예측 시간**: < 100ms

개인 소비 패턴이 일정할수록 정확도 향상!

---

## 🚀 배포 (선택)

### Streamlit Cloud (무료)

1. GitHub에 프로젝트 업로드
2. https://streamlit.io/cloud 접속
3. "New app" 클릭
4. Repository 선택 후 배포

### Docker
```bash
# Dockerfile 생성 후
docker build -t spending-predictor .
docker run -p 8501:8501 spending-predictor
```

---

## 📌 주의사항

1. **데이터 품질**: 최소 50개 이상의 거래 필요
2. **카테고리 매핑**: 개인 카테고리가 모델 카테고리와 다를 수 있음
3. **예측 한계**: 과거 패턴 기반이므로 새로운 패턴은 예측 어려움

---

## 📞 문의

문제가 발생하면 `03_models/06_sequence/metadata_*.json`에서 모델 상세 정보 확인
